import { clamp01 } from "../lib/utils";export function impliedNoVig(o1:number,o2:number){const inv1=1/o1,inv2=1/o2,s=inv1+inv2;return{p1:inv1/s,p2:inv2/s};}export function predictHome(pMarketHome:number){const x=Math.log((pMarketHome+1e-6)/((1-pMarketHome)+1e-6))*1.6;const p=1/(1+Math.exp(-x));return clamp01(p);}export function edge(pHome:number,pMarketHome:number){return pHome-pMarketHome;}